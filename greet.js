//create a function called greet that takes a name parameter log a greeting to that name in the function
//create a file called names.txt
//list 5 names (as strings) csv format in that file
function greet(name) {
    console.log(`Hello, ${name}`)
}
 
//run the greet.js script so that a greeting is said to all five names
// greet();
// greet();
// greet();
// greet();
// greet();

module.exports = greet;